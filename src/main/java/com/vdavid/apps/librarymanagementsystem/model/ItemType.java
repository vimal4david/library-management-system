package com.vdavid.apps.librarymanagementsystem.model;

//TO DO In memory database doesn't support ENUM. Migrate to a different DB
//and use this enum to restrict the item types
public enum ItemType {
    BOOK, CD, DVD, VHS
}
